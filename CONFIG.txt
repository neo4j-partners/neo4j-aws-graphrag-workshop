# Workshop Configuration
#
# Fill in the Neo4j credentials below.
# Fill in the Bedrock API key at the bottom of this file if your instructor
# gives you one.
# Every other setting has a working default and does not need to be modified.

# =============================================================================
# Neo4j AuraDB Free (REQUIRED - fill in during Setup)
# =============================================================================
# Enter the credentials from the AuraDB Free instance you created in Setup.
# They are shown once when the instance is created. Make sure you saved them.
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=your_password_here
NEO4J_DATABASE=neo4j

# =============================================================================
# AWS settings (pre-configured - no changes needed)
# =============================================================================
# The notebooks read this name and export it as AWS_DEFAULT_REGION during setup,
# because botocore reads only AWS_DEFAULT_REGION and would otherwise fall back to
# whatever region your active AWS profile configures.
AWS_REGION=us-east-1

# Model override. The workshop default is us.anthropic.claude-sonnet-4-6.
# MODEL_ID=us.anthropic.claude-sonnet-4-6

# =============================================================================
# Bedrock API key (only if the instructor gives you one)
# =============================================================================
# The instructor hands out a Bedrock API key at the start of class. Uncomment
# the line below and paste that key after the equals sign.
#
# Leave the line commented if you are not pasting a key. An uncommented blank
# value is worse than no line at all, because botocore then sends an empty
# Bearer header and never falls back to your normal AWS credentials. Every
# Bedrock call fails in that state.
#
# The key is a credential. Never commit it or share it outside the class.
# AWS_BEARER_TOKEN_BEDROCK=
