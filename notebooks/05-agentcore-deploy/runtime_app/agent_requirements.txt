# Pinned, not floated. The image is built once and then answers every
# invocation for the life of the Runtime, so a range that resolves differently
# next Tuesday changes what is deployed without anything in this repository
# changing. `notebooks/requirements.txt` pins Strands and neo4j-graphrag to
# these same versions, so the agent a participant builds locally in Module 3
# reads the same tool specifications as the one deployed here. The versions
# below are the ones the workshop is verified on.
strands-agents[otel]==1.53.0
bedrock-agentcore==1.22.0
aws-opentelemetry-distro==0.19.0
boto3==1.43.74
neo4j==6.2.0
neo4j-graphrag==1.19.0
